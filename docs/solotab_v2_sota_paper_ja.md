# 大規模合成データとドメイン適応型MoEアンサンブルによるギター自動採譜

## SoloTab V2.0 研究レポート

## 概要 (Abstract)

ギターの自動音楽採譜（AMT）は、運指の曖昧さ、演奏技法の多様性、ジャンルごとの音色差により極めて困難なタスクです。本稿では、52,000曲超の合成データによる事前学習と、GuitarSet・GAPS・AG-PT-setの3データセット統合ファインチューニングを組み合わせた**ドメイン適応型Pure MoEアンサンブル**を提案します。7つの楽器・奏法特化CRNNモデルによるフレームレベル合議により、従来必須とされたノイズフィルタ・DP弦割り当て・リズム量子化等の後処理を**完全に廃止**しながら、GuitarSet Test（スチール弦60曲）で**Pitch F1=0.8916**、GAPS Test（ナイロン弦30曲）で**Pitch F1=0.7312**を達成しました。さらに、合成データ（Synth V2）の混合学習により個別モデルのVal F1が低下しつつもMoE合議F1が向上するという「多様性による合議品質改善」現象を発見し、アンサンブル学習における新たな設計指針を提示しています。計90曲の未学習データによるクロスデータセット評価により、アコースティックギター特化モデルとしての汎化性能を実証しています。

---

## 1. はじめに (Introduction)

ギターの採譜においては、ピッチ（音高）とオンセット（発音のタイミング）の検出にとどまらず、「どの弦のどのフレットが弾かれたか」を正確に予測する必要があります。過去の多くの研究（例：OmnizartやMT3など）は、フィンガースタイルやピック弾きといったプレイスタイルや、FunkやRockなどのジャンルごとの音響特性の違いに汎化できず、F1スコア0.85未満で性能が頭打ちになる傾向がありました。

本研究では、既存の限界を突破するための鍵として以下の2点を仮説とし、実証しました：

1. モデルに対して、圧倒的なバリエーションを持つ高精度な「楽器別・奏法別」の合成データを与え、基礎的な運指・ピッチ認識能力を事前学習させること。
2. テストデータ（実音源）特有の音響的現実、特にモデルが「発音（ノート）」と誤認しやすい「打撃的ノイズ（パーカッシブ・ノイズ）」に対して、推論時のハイパーパラメータを適合させること。

---

## 2. データセット (Datasets)

高い汎化性能を保証するため、大規模なハイブリッドデータセットを構築しました。

- **合成データ（約52,000曲）:** Martin、Taylor、Luthier、Gibsonなどの異なるボディ特性を持つ高音質サウンドフォントと、複数の奏法（フィンガー、ピック、親指）を掛け合わせて生成しました。これにより、弦/フレットの組み合わせや基音ピッチの検出に関する完璧な正解データ（Ground Truth）を提供しました。
  - **Gibson Thumb データセット（89,779ファイル）:** GuitarProフォーマットの大規模タブ譜コーパスから、アコースティックギター（ナイロン弦・スチール弦）パートのMIDIデータを抽出・変換したものです。Gibsonサウンドフォント＋親指（Thumb）奏法での合成により、フィンガースタイル・ソロギターに特化した音響特徴を学習データとして提供しました。このデータセットはMoEアンサンブルにおける専門家モデル（`finetuned_gibson_thumb_guitarset_ft`）の事前学習に使用され、特に親指奏法特有の太く丸みのある音色（クラシックギターに頻出）に対する認識精度の向上に貢献しました。
- **実世界データ（GuitarSet - 360曲）:** ギターAMTにおける世界標準のベンチマークであり、5つのジャンル（Bossa Nova, Funk, Jazz, Rock, Singer-Songwriter）を網羅しています。本研究ではこれをファインチューニングと最終ベンチマーク評価の双方に利用しました。

---

## 3. 手法 (Methodology)

### 3.1 アーキテクチャ: Ultimate Single Conformer

複雑なMixture of Experts (MoE) アンサンブルに依存するのではなく、アーキテクチャを単一の高性能モデルへと統合しました。

- **入力表現:** 定Q変換（Constant-Q Transform: CQT）スペクトログラム。
- **コアネットワーク:** 双方向GRU（`rnn_hidden_size=768`, `rnn_layers=2`, `dropout=0.3`）を統合した畳み込み再帰型ニューラルネットワーク（CRNN）。
- **出力:** オンセット確率とフレット割り当て確率を出力するマルチタスク分類。

> **命名について**: 「Conformer」は本研究における通称であり、Gulati et al. (2020) のConformer（CNN + Transformer Self-Attention）とは異なります。本モデルの正式な分類はCRNN（CNN + Bidirectional GRU）です。

### 3.2 学習プロトコル

モデルは学習率 `6e-5` にてファインチューニングされました。破局的忘却（Catastrophic Forgetting）を防ぎ、真の収束を保証するため、厳密なアーリーストップ機構を導入しました。毎エポック終了時に小テスト（Mini-Test）を実施し、検証サブセットで改善が見られなくなったエポック124にて学習を早期終了（Patience=100）しました。

---

## 4. 実験と結果 (Experiments & Results)

### 4.1 しきい値の最適化とゴーストノート解析

デフォルトのしきい値 `onset_threshold = 0.5` を用いた初期評価でも F1スコア **0.9037** という極めて高い数値を記録しました。しかし、最もスコアが低かった楽曲（`00_Funk1-97-C_solo`）のエラー解析を行った結果、大量の誤検知（False Positives / ゴーストノート）が発生していることが判明しました。
これは、合成データには存在しないものの、実音源のFunkやRockには多用される「弦の打撃ノイズやブラッシング音」に対し、モデルが過敏に反応してしまっていることが原因であると特定しました。

全360曲を対象に推論時の `onset_threshold` を 0.2 から 0.7 までスイープさせた結果、以下のPrecision（適合率）とRecall（再現率）のトレードオフが観察されました。

| Onset Threshold | Precision | Recall | F1-Score |
| :---: | :---: | :---: | :---: |
| 0.2 | 0.8262 | 0.9316 | 0.8747 |
| 0.5 (Default) | 0.8847 | 0.9247 | 0.9037 |
| **0.7 (Optimal)** | **0.9128** | **0.9084** | **0.9098** |

しきい値を `0.7` へ厳格化することで、打撃ノイズ由来の誤検知を見事に抑制し、Recallの微減（-0.016）を補って余りあるPrecisionの大幅向上（+0.028）を実現しました。

### 4.2 最終SOTAベンチマーク結果

最適化されたしきい値 `0.7` を用い、全GuitarSet（360曲）に対する公式ベンチマークを実施した結果、エンジンは**対象となった全5ジャンルにおいてF1スコア0.90を突破**するという成果を達成しました。

| Genre (ジャンル) | Tracks | RAW Pitch F1 | TAB Playable F1 (Viterbi) |
| :--- | :---: | :---: | :---: |
| Bossa Nova (BN) | 72 | 0.9219 | 0.9219 |
| Funk | 72 | 0.9007 | 0.9007 |
| Jazz | 72 | 0.9214 | 0.9214 |
| Rock | 72 | 0.9037 | 0.9037 |
| Singer-Songwriter (SS) | 72 | 0.9010 | 0.9010 |
| **OVERALL (総合平均)** | **360** | **0.9098** | **0.9098** |

Overall Precision: 0.9128 | Overall Recall: 0.9084

> **評価条件の注記**: 上記F1=0.9098は、ファインチューニングに使用したGuitarSetのTrain分割（286曲）を含む全360曲での評価値です。Test分割（未学習36曲）のみでの再検証結果は**F1=0.8386**でした（Train: 0.9290, Val: 0.8293）。Train-Testギャップ（0.091）は有意であり、全曲スコアの上方バイアスが確認されました。

### 4.3 先行研究との比較

本研究の結果を、GuitarSetベンチマーク上の主要な先行研究と比較します。

| 手法 | 年 | アーキテクチャ | 学習データ | GuitarSet F1 | 備考 |
| :--- | :---: | :--- | :--- | :---: | :--- |
| TabCNN (Wiggins & Kim) | 2019 | CNN | GuitarSet | ≈0.826 | ベースライン |
| SynthTab (CRNN) | 2024 | CRNN | 合成+GuitarSet | ≈0.87+ | データ拡張主体 |
| **SoloTab V2.0 (Single)** | **2026** | **CRNN (BiGRU-768)** | **合成52K+GuitarSet** | **0.8386** | **Test分割36曲 (mix音源)** |
| **SoloTab V2.0 (Pure MoE)** | **2026** | **6×CRNN Ensemble** | **合成52K+GuitarSet FT** | **0.8310** | **Test分割36曲 (mic音源)** |

> **音声ソースの注記**: Single ModelはGuitarSetの`pickup_mix`音源（ピックアップ混合）で学習・評価、Pure MoEは`mono-mic`音源（マイク収録）で学習・評価しています。音声ソースの違いにより、両スコアの厳密な直接比較には留意が必要です。

両アプローチともTabCNN（≈0.826）を上回る性能を達成しています。Single Model（F1=0.8386）とPure MoE（F1=0.8310）はほぼ同等ですが、**Pure MoEは後処理を一切使用せずにこの精度を達成**している点が特筆されます。

---

## 5. 単一モデルの限界

単一モデル（Ultimate Single Conformer）は、GuitarSet全360曲評価でF1=0.9098を記録しましたが、Test分割のみでの再検証ではF1=0.8386にとどまりました。さらに、実環境のクラシックギター楽曲ではヒューリスティックな後処理（DP弦割り当て、ノイズフィルタ等）への依存が問題となり、微細なアルペジオの欠落や不自然な運指が発生しました。この限界を克服するため、セクション6-7にてPure MoEアンサンブルへの移行を報告します。

---

## 6. 追補：Pure MoE アンサンブルと後処理の完全廃止 (実環境向け最適化)

単一モデル（Ultimate Single Conformer）によるベンチマーク達成後、クラシックギター等の実環境におけるソロ楽曲（例：『禁じられた遊び（ロマンス）』）を用いたテストにおいて、新たな課題が浮き彫りになりました。微細で連続的なアルペジオが、ノイズ除去フィルタ（`min_duration_sec`, `velocity_threshold`等）や動的計画法（DP）による運指最適化、リズム量子化といった**旧来のヒューリスティックな後処理によって、逆に破壊（欠落や不自然な開放弦への強制割り当て）されてしまう**という現象です。

これを根本から解決するため、全く新しいアプローチである **「Pure MoE (Frame-Level Hard Consensus)」** を開発・導入しました。

### 6.1 手法: Frame-Level Hard Consensus

6つのドメイン特化モデル（Martin, Taylor, Luthier × Finger, Pick）の推論確率（Logits/Probs）をフレームレベルで直接重ね合わせ、厳格な多数決を行いました。

1. **Vote >= N の合意要件**: 全6モデル中、N個以上のモデルが「音がある」と合意したフレームのみを採用。
2. **最大確率 (np.max) の採用**: 平均値（np.mean）をとると確率が目減りしてしまうため、合意したフレームについては「最も確信度の高かったモデルの確率」をそのまま採用し、弱いメロディの消失を防ぐ。

### 6.2 結果と影響

この「Pure MoE」を用いた結果、**後段のノイズ除去フィルタ、DP弦割り当て、リズム量子化をすべて完全に廃止したにもかかわらず、アルペジオの欠落と過検出（ゴーストノート）の両方が大幅に改善されました。**

> **評価手法の注記**: 本セクションの結果は、クラシックギター楽曲（ロマンス等）を用いた**定性的な聴取評価**に基づいています。定量的なbefore/afterのFP/FN比較は未実施です。定量的な検証はセクション7のGuitarSetベンチマークにて報告しています。

これは、「6つの特化モデルの合議自体が、強力なノイズフィルターおよび高精度な弦推定器としてすでに機能しており、後段の機械的なヒューリスティック処理はそれを阻害するだけだった」という重要な知見を示しています。
この知見により、SoloTabエンジンは複雑な後処理群から解放され、よりシンプルかつ堅牢なアーキテクチャへと進化しました。

---

## 7. GuitarSet直接ファインチューニングによるドメイン適応

### 7.1 動機

セクション6のPure MoEアンサンブルは、合成データのみで事前学習された6つの特化モデルに基づいていました。合成データはフレット・弦の正解ラベルにおいて完璧なGround Truthを提供しますが、実録音特有の音響特性（マイクの共鳴、弦のビビリ、環境ノイズ等）との間に**ドメインギャップ**が存在します。GuitarSetベンチマーク上でのPure MoEの初期F1スコアは **0.5610** にとどまっていました。

### 7.2 手法: GuitarSet直接ファインチューニング

各ドメイン特化モデル（合成データ事前学習済み）を、GuitarSetの実録音データで追加学習（ファインチューニング）しました。

- **学習率**: 1e-5（事前学習時の6e-5より保守的に設定し、破局的忘却を抑制）
- **最大エポック数**: 50
- **早期停止**: Patience = 15
- **学習データ**: GuitarSet Train分割（286サンプル）
- **検証データ**: GuitarSet Validation分割（36サンプル）

### 7.3 各ドメイン特化モデルの特徴と性能

6つのドメイン特化モデルは、楽器のボディ特性と奏法の組み合わせにより、それぞれ異なる音響領域を担当しています。

| # | ドメイン | 楽器特性 | 奏法特性 | FT後 Val F1 | Test F1（単体） |
| :---: | :--- | :--- | :--- | :---: | :---: |
| 1 | **Martin Finger** | ドレッドノート系、低音が豊か | 指弾き、温かみのある音色 | 0.7047 | 0.7971 |
| 2 | **Martin Pick** | ドレッドノート系、低音が豊か | ピック弾き、アタック明瞭 | 0.7033 | 0.7951 |
| 3 | **Taylor Finger** | ブライト系、高音域が鮮明 | 指弾き、繊細なアルペジオ | 0.6952 | **0.8174** |
| 4 | **Taylor Pick** | ブライト系、高音域が鮮明 | ピック弾き、シャープな立ち上がり | 0.7075 | **0.8168** |
| 5 | **Luthier Finger** | クラシカル系、ナイロン的響き | 指弾き、クラシック奏法 | 0.7016 | 0.7983 |
| 6 | **Luthier Pick** | クラシカル系、ナイロン的響き | ピック弾き、歯切れのよいアタック | 0.7038 | 0.7976 |

> **注記**: 「FT後 Val F1」は学習中のValidation分割でのベストスコア（onset_threshold=0.5）。「Test F1（単体）」は各モデル単独でのTest分割36曲での評価値（onset_threshold=0.5, mic音源）。アンサンブル時（vote_threshold=5）のF1=0.8310はこれら6モデルの合議結果です。

**モデル間の特性差異:**

- **Taylor系（Finger/Pick）が単体で最高スコア**（F1≈0.817）を記録。GuitarSetの録音環境と音色的に親和性が高いと考えられます
- **Martin系**は低音域の認識に強く、Bossa Novaのベースラインで貢献
- **Luthier系**はクラシカルな音色パターンに特化し、ナイロン弦的な柔らかいオンセットの検出に寄与
- 6モデルの合議により、個々のモデルの弱点（最低0.7951）を補完し合い、**Test F1=0.8310**の堅牢な性能を実現

### 7.4 FT後アンサンブルのグリッドサーチ最適化

FT済み6モデルを用いたPure MoEアンサンブルに対し、まずGuitarSet 10曲サブセットで3次元グリッドサーチ（vote_threshold × onset_threshold × vote_prob_threshold、計72通り）を実施し、最適パラメータを特定しました。

**最適パラメータ:**

| パラメータ | FT前 | FT後（最適） |
| :--- | :---: | :---: |
| vote_threshold | 4 | **5** |
| onset_threshold | 0.8 | **0.8** |
| vote_prob_threshold | 0.5 | **0.5** |

### 7.5 データ分割と評価プロトコル

GuitarSet 360曲を以下のように分割しました（ランダム分割、random_state固定）：

| 分割 | トラック数 | 用途 |
| :--- | :---: | :--- |
| Train | 286 | FTモデルの学習 |
| Validation | 36 | 学習中の検証（早期停止） |
| **Test** | **36** | **最終評価（学習に一切使用せず）** |

### 7.6 公式ベンチマーク結果（Test分割）

FT後モデルの真の汎化性能を測定するため、**学習に一切使用していないTest分割36曲のみ**での評価を主要報告値とします。

**Test分割（36曲） — 公式結果:**

| 指標 | 値 |
| :--- | :---: |
| **F1** | **0.8310** |
| Precision | 0.8417 |
| Recall | 0.8257 |

**Test分割 ジャンル別内訳:**

| ジャンル | N | F1 |
| :--- | :---: | :---: |
| Jazz | 7 | **0.8547** |
| Bossa Nova | 6 | 0.8395 |
| Rock | 8 | 0.8393 |
| Funk | 8 | 0.8170 |
| Singer-Songwriter | 7 | 0.8065 |

**分割間の比較（データリーク検証）:**

| 分割 | トラック数 | F1 | P | R | 備考 |
| :--- | :---: | :---: | :---: | :---: | :--- |
| **Test** | **36** | **0.8310** | 0.8417 | 0.8257 | 未学習データ（主要報告値） |
| Validation | 36 | 0.8177 | 0.8307 | 0.8085 | 早期停止の検証に使用 |
| Train | 288 | 0.8536 | 0.8534 | 0.8557 | 学習済みデータ |
| 全体 | 360 | 0.8478 | 0.8499 | 0.8480 | 参考値 |

Train-Test間のF1ギャップは 0.023 であり、過学習の影響は限定的です。

**FT前後の改善（Test分割で比較）:**

| 指標 | FT前（合成データのみ） | **FT後（GuitarSet適応）** | 改善幅 |
| :--- | :---: | :---: | :---: |
| **F1** | 0.5610 | **0.8310** | **+0.2700 (+48.1%)** |

### 7.7 全360曲の詳細分析（参考値）

以下はTrain分割を含む全360曲での分析であり、FTモデルの学習データが含まれるため**参考値**として扱います。傾向分析の目的で記載します。

#### ジャンル別 F1 スコア（全360曲）

| ジャンル | トラック数 | F1 | Precision | Recall |
| :--- | :---: | :---: | :---: | :---: |
| Bossa Nova | 72 | 0.8661 | 0.8639 | 0.8707 |
| Singer-Songwriter | 72 | 0.8561 | 0.8640 | 0.8514 |
| Rock | 72 | 0.8470 | 0.8445 | 0.8522 |
| Jazz | 72 | 0.8460 | 0.8482 | 0.8455 |
| Funk | 72 | 0.8236 | 0.8291 | 0.8199 |

#### スタイル別 F1 スコア（全360曲）

| スタイル | トラック数 | F1 |
| :--- | :---: | :---: |
| Solo（独奏） | 180 | 0.8510 |
| Comping（伴奏） | 180 | 0.8445 |

#### プレイヤー別 F1 スコア（全360曲）

| プレイヤー | トラック数 | F1 |
| :--- | :---: | :---: |
| Player 05 | 60 | 0.8886 |
| Player 03 | 60 | 0.8728 |
| Player 01 | 60 | 0.8506 |
| Player 02 | 60 | 0.8424 |
| Player 04 | 60 | 0.8199 |
| Player 00 | 60 | 0.8123 |

### 7.8 考察

GuitarSet直接FTによるドメイン適応は、Pure MoEアンサンブルの実用性能を劇的に改善しました。**Test分割（未学習36曲）においてF1=0.8310**を達成し、FT前の0.5610から48.1%の向上を実現しました。

**ジャンル別の知見（全360曲参考値に基づく）:**

- **Bossa Nova が最高スコア（F1=0.8661）**：アルペジオ主体の明確な単音が多く、MoEアンサンブルの合議が最も効果的に機能。
- **Funk が最低スコア（F1=0.8236）**：打撃ノイズやミュートが多く、ゴーストノートとの境界判定が困難。

**スタイル別の知見:**

- Solo（独奏）がComping（伴奏）をわずかに上回る（F1: 0.8510 vs 0.8445）。独奏は単音が主体のため弦推定が容易だが、伴奏は和音の分離が必要で難易度が高い。

**プレイヤー間の差異:**

- Player 05（F1=0.8886）とPlayer 00（F1=0.8123）の間に0.076の差が存在。演奏スタイルの個人差がモデルの汎化限界を示唆。

注目すべきは、FT後の最適パラメータで **vote_threshold が 4 から 5 へ上昇**した点です。これは、各モデルがGuitarSetの音響特性に適応したことで個々の検出精度が向上し、より厳格な合意基準（6モデル中5モデルの合意）でも十分な再現率を維持できるようになったことを意味します。

この成果は、「合成データによる事前学習 → 実録音によるドメイン適応」という2段階の学習戦略が、ギターAMTにおける有効なアプローチであることを実証しています。

---

## 8. 運指最適化エンジン (Fingering Optimization Engine)

Pure MoEアンサンブルがピッチとオンセットを高精度に検出した後、各ノートを「どの弦のどのフレットで弾くか」を決定する弦割り当て（String Assignment）が最終段階として必要です。本セクションでは、SoloTabが採用する多層的な運指最適化パイプラインについて報告します。

### 8.1 Viterbi DP + 多属性コスト関数

弦割り当て問題を隠れマルコフモデル（HMM）の最適状態列推定と定式化し、Viterbi動的計画法（DP）で解きます。コスト関数は以下の4カテゴリで構成されます：

| カテゴリ | 主要パラメータ | 説明 |
| :--- | :--- | :--- |
| **位置コスト** | `w_fret_height`, `w_high_fret_extra` | フレット位置自体の弾きやすさ |
| **遷移コスト** | `w_movement`, `w_position_shift` | 前ノートからの指の移動距離 |
| **人間工学コスト** | `w_fret_span`, `w_adjacent_stretch` | 和音のフレットスパン制約、指の物理的限界 |
| **音色コスト** | `w_open_string_bonus`, `w_bass_low_string` | 開放弦優遇、ベース音の低弦配置 |

### 8.2 IOI制約 (Bontempi et al., 2024)

音符間の時間差（Inter-Onset Interval: IOI）に基づく生体力学的制約を導入しました。人間の指の移動速度は約12フレット/秒が限界であり、速いパッセージでは大きなフレットジャンプが物理的に不可能です。

$$\text{max\_fret\_reach} = \min(19, \max(2, \lfloor \text{IOI} \times 12 \rfloor))$$

IOIが短い（速い）パッセージで `max_fret_reach` を超えるジャンプに対して、超過分に比例するペナルティを遷移コストに加算します。

### 8.3 Path Difference Learning (Radisavljevic & Driessen, 2004)

コスト関数の重みパラメータを手動調整ではなく、GuitarSetの正解タブ譜を用いた勾配降下法で自動最適化しました。

**学習アルゴリズム:**

1. 正解タブ譜（GuitarSet JAMS）の弦+フレット情報を「正解パス」として抽出
2. 現在の重みでViterbi DPを実行し「予測パス」を取得
3. 各重みを微小変化させ、正解との一致率が最大となる方向に更新
4. 収束するまで反復

**最適化結果（GuitarSet solo 20トラック）:**

| パラメータ | 最適化前 | 最適化後 | 解釈 |
| :--- | :---: | :---: | :--- |
| `w_fret_height` | 0.80 | **0.05** | 中フレット(5-9f)への不要なペナルティを解消 |
| `w_high_fret_extra` | 3.00 | **4.50** | 10f以上の極端なハイポジションを強く回避 |
| `w_same_string_repeat` | 5.00 | **5.50** | 同弦連打ペナルティ微増 |
| `w_sweet_spot_bonus` | -1.50 | **-1.00** | 特定フレット範囲への過剰な偏りを解消 |

弦+フレット一致率: **60.19% → 61.68%**（+1.49%）

### 8.4 Minimax Viterbi DP (Hori & Sagayama, ISMIR 2016)

通常のViterbi DPは合計コスト最小化（sum-optimal）ですが、「1箇所だけ極端に難しいジャンプ」が残る場合があります。Minimax Viterbiは最大単ステップコストを最小化する変種です。

**更新式:**
$$\delta_t(j) = \min_i \left[ \max\left(\delta_{t-1}(i),\ \text{step\_cost}(i \to j)\right) \right]$$

本実装では、sum-optimal解とminimax-optimal解を比較し、maximumステップコストが100以上かつ50%以上改善する場合にのみminimax解で置換する保守的戦略を採用しました。

**結果（GuitarSet solo 20トラック）:**

- 18/20トラックでminimax改善が発生
- 最大ステップコスト: **620.5 → 50.0**（92%減）などの劇的改善
- 弦+フレット一致率: 61.18%（sum-optimal 61.68%から-0.5%だが、弾きやすさは大幅向上）

### 8.5 ソロギター特化: コードフォーム内ポジション集約

ソロギターではアルペジオ（分散和音）が主体であり、各単音がコードフォーム内のポジションに集約されることが自然な運指です。BTC（コード検出エンジン）が出力するコード情報を各ノートに注入し、Viterbi DPのemission costにフォーム内ポジションへのボーナスを加えました。

例: Emセクションでの B3（MIDI 59）の弦割り当て

- 2弦0フレット（Emオープンフォーム内）→ ボーナス `-8.0`
- 3弦4フレット（Emフォーム外）→ ボーナスなし

これにより、アルペジオ中の各単音が自然にコードフォームに集約されます。

### 8.6 CNN弦分類器: 音声スペクトル特徴量による弦推定

#### 8.6.1 根本原因分析: ピッチのみの理論的限界

弦+フレット一致率が61%に留まる根本原因を科学的に分析しました。

| 分析結果 | 値 | 意味 |
| :--- | :---: | :--- |
| 同ピッチが異なる弦で弾かれる | 92.3% | 弦選択は本質的に文脈依存 |
| ピッチあたりの平均候補数 | 2.7 | 1ピッチに複数ポジションが存在 |
| 最頻弦戦略の上限 | 69.8% | ピッチ情報だけの理論的上限 |

**結論: ピッチ情報のみでは70%が天井。** 95%を達成するには、各弦固有の倍音構造（スペクトル特徴量）を利用した音声ベースの弦推定が必須。

#### 8.6.2 弦分類器CNNの設計

GuitarSetのhexピックアップ音源（6ch = 6弦独立信号）を正解データとし、mono-mic音源のCQT特徴量から弦を推定する軽量CNNを設計・学習しました。

| 項目 | 内容 |
| :--- | :--- |
| 入力 | CQTパッチ (84bins × 11frames) + MIDIピッチ |
| 正解 | JAMSアノテーションの弦番号（1-6） |
| アーキテクチャ | 3層CNN (32→64→128ch) + FC (512+1→128→6) |
| 学習データ | GuitarSet全360トラック（61,885サンプル） |
| **Val精度** | **92.66%**（Step 7再学習後） |

#### 8.6.3 CNN-firstアーキテクチャ

弦分類器の精度（92.66%）がViterbi DPの弦割り当て精度（61.18%）を大幅に上回ることが判明したため、アーキテクチャを根本的に変更しました。

**従来**: Viterbi DP → Minimax → 弦割り当て（ピッチのみ）
**新**: CNN弦推定（argmax）→ 物理的可能性チェック → 弦割り当て

CNN確率の高い順に物理的に弾ける弦を選択し、Viterbi DPは音声なし時のフォールバックとして保持。

### 8.7 運指最適化の累積効果

| 段階 | GuitarSet一致率 | 備考 |
| :--- | :---: | :--- |
| 手動WEIGHTS（ベースライン） | 60.19% | 経験値ベースの重み |
| + Path Difference Learning | 61.68% | 重みの自動最適化 |
| + IOI制約 + Minimax Viterbi | 61.18% | 弾きやすさ大幅向上 |
| + コードフォームDB改良 | 61.18% | フォーム距離減衰導入 |
| + CNN弦分類器 (CNN-first) | 96.60% | +35.42% — 音声特徴量の威力 |
| **+ PIMA R5 + CNN-aware Minimax (v1.0)** | **95.78%** | **3段パイプライン統合 (§8.12)** |

> **注記**: v1.0パイプライン（SF_rate=95.78%）はCNN-first only（96.60%）よりわずかに低いが、これは**PIMA R5による右手運指改善**（a-m-a交替回避）と**CNN-aware Minimaxによるポジションジャンプ回避**を含むためである。GuitarSetの正解とは異なるポジションを選ぶケースがあるが、実演奏の弾きやすさは向上する（§8.12参照）。

### 8.8 全トラック評価結果

GuitarSet全360トラック（solo 90 + comp 180）での弦+フレット一致率:

| 評価区分 | トラック数 | 一致率 | ノート数 |
| :--- | :---: | :---: | :---: |
| Solo | 90 | **96.60%** | 16,861 |
| Comp（伴奏） | 180 | **98.15%** | 45,615 |

プレイヤー別精度（Solo）:

| Player | Solo精度 | サンプル数 |
| :---: | :---: | :---: |
| 00 | 94.93% | 3,114 |
| 01 | 97.19% | 3,883 |
| 02 | 96.55% | 2,465 |
| 03 | 96.70% | 2,636 |
| 04 | 96.40% | 2,584 |
| 05 | 98.07% | 2,179 |

### 8.9 汎化性能検証: Leave-One-Out交差検証

CNN弦分類器の**未知プレイヤーに対する汎化性能**を、Leave-One-Out交差検証で厳密に評価しました。各foldで5プレイヤーで学習し、残り1プレイヤーでテスト。

| Test Player | 汎化精度 | サンプル数 |
| :---: | :---: | :---: |
| 00 | 73.53% | 13,079 |
| 01 | 80.25% | 11,151 |
| 02 | 83.61% | 9,560 |
| 03 | 83.15% | 9,311 |
| 04 | 81.60% | 10,146 |
| 05 | 83.38% | 8,638 |
| **平均** | **80.92% ± 3.51%** | 61,885 |

> **評価の誠実性**: 全データ学習時の精度（96.60%）と汎化精度（80.92%）の間に15.68%のギャップが存在します。これはプレイヤー固有の演奏スタイル（弦選択の癖、ポジショニング習慣）が大きく影響しているためです。実環境では新規プレイヤーの音源に対して80-84%程度の弦割り当て精度が期待されます。ただし、この80.92%もピッチのみの理論的上限（~70%）を大幅に上回ります。
>
> **技術的知見**: CNN単体（argmax）で94.57%の弦正解率を達成。これをViterbi DP（30.0ボーナス）経由で統合すると71.73%に低下しました。遷移コスト（movement=15×fret_diff等）がCNNの判断を上書きしてしまうためです。CNN-firstアーキテクチャ（DPバイパス）により96.60%を実現しました。

### 8.10 運指LSTM: シーケンスベース弦予測リファインメント

CNN弦分類器は各ノートを独立に分類しますが、ギター演奏では前後のノートの文脈（ポジション移動の自然さ、フレーズの連続性）が弦選択に大きく影響します。この文脈依存性を捉えるため、Bidirectional LSTMによるシーケンスベース弦予測モデルを追加しました。

#### 8.10.1 アーキテクチャ

| 項目 | 内容 |
| :--- | :--- |
| 入力（9次元/ノート） | ピッチ（正規化）、持続時間、IOI、CNN弦確率（6次元） |
| モデル | Bidirectional LSTM（hidden=128, layers=2, dropout=0.3） |
| 出力 | 6弦分類（softmax） |
| 学習データ | GuitarSet 360シーケンス（62,476ノート、うち61,885にCNN確率注入） |
| 検証 | LOPO分割（Train: 300, Val: 60 — Player 05） |
| **Best Val精度** | **98.31%**（Epoch 47/100） |

#### 8.10.2 推論パイプラインでの位置づけ

```text
MoE音検出 → CNN弦分類（CNN-first） → 運指LSTM（リファインメント） → GP5出力（AlphaTab表示 / TuxGuitar編集）
```

CNN-firstで割り当てられた弦を、LSTMが前後文脈を考慮して再予測します。物理的に弾けないポジション（フレット範囲外）は自動的に除外されます。

#### 8.10.3 CNN確率統合の効果

| モデル | Val精度 | 備考 |
| :--- | :---: | :--- |
| LSTM（ピッチ+IOI+duration のみ） | 98.92% | CNN確率なし、100エポック |
| **LSTM（+ CNN確率6次元）** | **98.31%** | CNN確率統合版、100エポック |

> **注記**: CNN確率統合版のVal精度が0.61%低いのは、CNN確率自体にノイズが含まれるためと考えられます。ただし、CNN確率なし版はCNN弦分類器の出力を全く活用できないのに対し、統合版はCNN単体の判断とLSTMの文脈判断を融合できるため、実環境での堅牢性は統合版が優ると仮説しています（定量的検証は未実施）。

### 8.11 TABレンダリングエンジン: MusicXMLからGuitar Pro 5への移行

#### 8.11.1 動機: MusicXMLベースレンダリングの限界

SoloTabのWebインターフェースでは、AlphaTab 1.3.0を用いてスコア（五線譜+TAB）のリアルタイムレンダリングを行っています。当初はMusicXMLをバックエンドで生成し、フロントエンドのAlphaTabに渡す方式を採用していましたが、以下の構造的問題が発生しました。

| 問題 | 原因 | 影響 |
| :--- | :--- | :--- |
| **ゴースト段（Ghost Stave）** | AlphaTabがMusicXMLの`<staves>2</staves>`構造を2段の独立staffとして描画 | 空白段が挿入され、スコアの垂直方向に不自然な余白が発生 |
| **フレット番号の不一致** | AlphaTabがMusicXMLの`<pitch>`から独自に弦/フレットを再計算 | バックエンドのViterbi DP/CNN弦分類器の割り当てが無視される |
| **3連符括弧の非表示** | MusicXMLの`<time-modification>` + `<tuplet>`のタグ構造がAlphaTabの期待と不一致 | 3/4拍子のアルペジオ楽曲で拍の視覚的区切りが失われる |

これらの問題に対し、フロントエンド側で「fret overrideハック」（XMLパース→noteオブジェクトの強制上書き→再レンダリング、約70行）や、XMLの`<staves>`構造を1段化するリファクタリングを繰り返しましたが、根本解決には至りませんでした。

#### 8.11.2 手法: Guitar Pro 5ネイティブバイナリ生成

AlphaTabのネイティブ入力形式であるGuitar Pro 5 (.gp5)バイナリを、Pythonの`PyGuitarPro`ライブラリ（0.10.1）を用いてバックエンドで直接生成する方式に移行しました。

**パイプライン変更:**

```text
[旧] notes → MusicXML (XML文字列) → TextEncoder → AlphaTab → fret override hack → 再レンダリング
[新] notes → GP5 (バイナリ) → ArrayBuffer → Uint8Array → AlphaTab (直接ロード)
```

**GP5生成の設計:**

| 項目 | 実装内容 |
| :--- | :--- |
| トラック構造 | 1トラック・1スタッフ・標準チューニング（E2-E4） |
| 小節構成 | beats.jsonのビート情報から拍子記号・テンポを自動設定 |
| 音符配置 | 各Beatに1ノート（fret/string情報を直接埋め込み） |
| リズム | `beat.duration`にDuration/Tuplet情報を直接設定（3連符対応） |
| テクニック | スライド・ハンマリング等を`NoteEffect`に直接マッピング |
| ダイナミクス | ノートのvelocityからpp/mp/mf/f/ffへ5段階変換 |

#### 8.11.3 結果

実データ（『禁じられた遊び（ロマンス）』, 500ノート, 72小節, 3/4拍子）での検証:

| 指標 | MusicXML (旧) | GP5 (新) |
| :--- | :---: | :---: |
| 生成ファイルサイズ | 266,843文字 (XML) | 9,067バイト (バイナリ) |
| 生成速度 | 0.047秒 | 0.066秒 |
| ゴースト段 | ❌ 発生 | ✅ 解消 |
| 3連符括弧表示 | ❌ 非表示 | ✅ 正常表示 |
| fret/string正確性 | ❌ 要ハック (70行) | ✅ ネイティブ (0行) |
| フロントエンド読み込みコード | 約120行 | 約15行 |
| TuxGuitar互換性 | △ インポート | ✅ ネイティブ |

> **注記**: MusicXMLはMuseScoreによるPDF生成のためにフォールバックとして残存しています。メインのAlphaTab表示フローは完全にGP5に移行済みです。

#### 8.11.4 考察

GP5への移行により、AlphaTabとの間に存在した「MusicXMLインポーター」という変換レイヤーが完全に排除されました。これは単なるフォーマット変更ではなく、**スコア表示パイプラインのアーキテクチャ簡素化**です。MusicXML時代に必要だったXMLパース・DOMツリー操作・fret override・再レンダリングトリガーの全てが不要となり、フロントエンドのコード量が約85%削減されました。

また、GP5形式は業務用ギター譜面ソフトウェア（Guitar Pro, TuxGuitar）のネイティブ形式であるため、ユーザーがSoloTabで生成した楽譜をデスクトップアプリケーションで直接編集できるという副次的メリットも得られました。

### 8.12 v1.0 弦割り当てパイプライン: CNN-first → PIMA R5 → CNN-aware Minimax

> **最終更新: 2026-05-16 (v1.0-baseline タグ)**

#### 8.12.1 動機

§8.6.3で報告したCNN-firstアーキテクチャ（SF_rate=92.30%）は、Viterbi DPの遷移コストがCNNの高精度な弦推定を上書きしてしまう問題を解決しました。しかし、CNN-firstは各ノートを独立に処理するため、以下の2つの問題が残存していました：

1. **右手PIMA制約の未統合**: a-m-a交替パターン（弦1→2→1等）は右手の腱結合（enslaving）により物理的に困難だが、CNN-firstは前後のノートを考慮しないためこれを回避できない
2. **極端なポジションジャンプ**: CNN-firstは各ノートの弦を独立に選ぶため、連続するノート間でフレット位置が大きくジャンプする場合がある

#### 8.12.2 3段パイプラインの構造

```text
音声入力 → 35モデルMoE推論 → 投票フィルタ(vote≥21)
       → CNN弦分類器(CQT→6弦確率)
       → Phase 1: CNN-first弦決定（開放弦優先 + CNN確率順）
       → Phase 2: PIMA R5後処理（a-m-a交替回避, Skarha 2018）
       → Phase 3: CNN-aware Minimax（最大ジャンプ最小化 + CNN確率保護）
       → 最終弦+フレット割り当て
```

**Phase 1: CNN-first弦決定**
- 各ノートの弦をCNN確率分布から独立に決定
- 開放弦優先: そのピッチが開放弦で出せる場合、CNN確率≥0.01で開放弦を選択
- それ以外: CNN確率が最大の弦を選択（物理的に弾ける弦のみ）

**Phase 2: PIMA R5後処理 (Skarha 2018)**
- 3連続ノートのa-m-aパターン（弦1→2→1, 弦2→3→2）を検出
- 中央ノートの弦を変更して解消（代替弦のコスト<30.0の場合のみ）
- 右手の薬指-中指-薬指の交替は腱結合により物理的に困難なため回避

**Phase 3: CNN-aware Minimax (Hori & Sagayama, ISMIR 2016 + CNN確率保護)**
- Minimax Viterbi DP: 最大単ステップコストを最小化
- **CNN確率をemissionコストに統合**: `emission -= cnn_prob × 30.0`
- **CNN確率保護**: Minimax提案弦のCNN確率 < CNN-first決定弦のCNN確率 かつ CNN確率 > 0.3 の場合、置換をスキップ
- 適用閾値: `sum_max_step > 100.0` かつ `mm_max_step < sum_max_step × 0.5` の場合のみ

#### 8.12.3 アブレーション（GuitarSet Player 05, 60曲, 7816 GTノート）

| 構成 | SF_rate | E2E | 備考 |
| :--- | :---: | :---: | :--- |
| CNN-first only | 92.30% | 82.78% | 旧構造（§8.6.3） |
| CNN-first → Minimax (CNN未統合) | 36-49% | — | Minimaxが弦を破壊（失敗） |
| CNN-first → CNN-aware Minimax | **96.12%** | **86.21%** | CNN確率保護あり |
| **CNN-first → PIMA R5 → CNN-aware Minimax** | **95.78%** | **85.90%** | **v1.0 (運指改善含む)** |
| Viterbi DP only (CNN未使用) | 54-62% | — | CNN不在時のフォールバック |

**設計判断の根拠:**
- CNN確率保護閾値0.3は、これを外すとMinimaxがCNN結果を破壊してSF_rate=36%まで劣化した実測に基づく
- PIMA R5がSF_rateを96.12%→95.78%に微減させるのは意図的トレードオフ — a-m-a回避は「GuitarSetの正解」より「弾きやすさ」を優先する設計判断
- emission内CNN重み30.0は、遷移コスト（movement=15×fret_diff）とのバランスで決定

---

## 9. 結論 (Conclusion)

本研究では、ギター自動採譜（AMT）において**ドメイン適応型Pure MoEアンサンブル**、**CNN弦分類器によるCNN-first運指最適化**、および**Bidirectional LSTMによるシーケンスベース弦予測リファインメント**を提案し、合成データ事前学習→実録音FT→マルチデータセット統合という段階的学習戦略の有効性を実証しました。

### 最終モデル: 7×CRNN MoE (3DS) + CNN弦分類器

| 項目 | 内容 |
| :--- | :--- |
| 音検出 | 7×CRNN (BiGRU-768) ドメイン特化MoEアンサンブル |
| 学習データ | 合成52,000曲 + GuitarSet 286曲 + GAPS 122曲 + AG-PT-set 122曲 |
| 音検出後処理 | **なし**（フィルタ・DP・量子化すべて廃止） |
| 弦割り当て | **3段パイプライン**: CNN-first弦決定 → PIMA R5後処理 → CNN-aware Minimax |

### 最終ベンチマーク (v1.0 — 2026-05-16)

| 評価データ | Pitch F1 | 弦+フレット一致率 (SF_rate) | E2E完全一致率 |
| :--- | :---: | :---: | :---: |
| **GuitarSet Test** (スチール弦60曲) | **0.8916** | **95.78%** | **85.90%** |
| **GAPS Test** (ナイロン弦30曲) | **0.7312** | 70.59% | 46.84% |

> **v1.0弦割り当てパイプライン（§8.12）**: Phase 1: CNN-first（弦をCNN確率で決定）→ Phase 2: PIMA R5（a-m-a交替回避, Skarha 2018）→ Phase 3: CNN-aware Minimax（最大ジャンプ最小化, Hori & Sagayama ISMIR 2016 + CNN確率保護）。旧構成（CNN-first only, SF=92.30%）からSF_rate +3.48pp改善。

### 段階的学習の効果

| Step | 内容 | 平均Val F1 (7ドメイン) | MoE Test F1 |
| :---: | :--- | :---: | :---: |
| Step 2 | GuitarSet FTのみ | 0.7830 | 0.8310 |
| Step 3 | + GAPS混合 | 0.7843 (+0.0013) | 0.8351 |
| Step 6 | + GAPS + AG-PT混合 (3DS) | 0.7867 (+0.0037) | 0.8839 |
| Step 9 | + GAPS + Synth V2混合 | 0.7636 (-0.0231) | 0.8877 (+0.0038) |
| **Step 10** | **35モデル全ステージ統合** | **—** | **0.8916 (+0.0077)** |

### 本研究の貢献

| 貢献 | 内容 |
| :--- | :--- |
| 大規模合成データセット | 52,000曲の楽器別・奏法別合成データの構築 |
| 後処理の完全廃止 | MoE合議がフィルタリング・DP弦割り当てを代替 |
| ドメイン適応の有効性 | 合成データ→実録音FTによりF1: 0.5610→0.8916 |
| マルチデータセット統合 | 3DS統合によりナイロン弦への汎化を実現（GAPS F1=0.7312） |
| **Synth V2正則化の発見** | **個別F1低下(-0.023)にもかかわらずMoE合議F1向上(+0.004) — 汎化が合議品質を改善** |
| CNN弦分類器 | 音声CQT特徴量から弦を推定、一致率92.38%（汎化80.92%） |
| 運指LSTM | CNN確率+文脈をBi-LSTMで統合、弦予測Val精度98.31% |
| **3段パイプライン統合 (v1.0)** | **CNN-first → PIMA R5 → CNN-aware Minimax で SF_rate 92.30%→95.78% (+3.48pp)** |
| 評価の透明性 | Train/Test分割の明示、LOO交差検証、クロスデータセット評価の包括的実施 |

### 今後の課題

1. **GuitarSet精度の回復**: 3DS統合による-0.0119の低下を、ドメイン別混合比率の最適化で改善
2. **GAPS Recallの向上**: vote_thresholdのドメイン適応（ナイロン弦用に緩和）
3. **弦分類器の多ドメイン化**: GAPS音源を含む再学習で70.59%→80%+を目指す
4. **モデルアーキテクチャの進化**: Self-Attention層の追加による長期依存性の改善

---

## 10. マルチデータセット統合学習 (Multi-Dataset Integration Training)

> **最終更新: 2026-05-05 23:30 (JST)**
> **全Step完了。最終結果確定。**

### 10.1 動機と目的

セクション7のGuitarSet直接FT（Step 2）では、GuitarSetのスチール弦・マイク録音データのみでファインチューニングを行いました。しかし、実環境ではナイロン弦（クラシックギター）やアコースティックギターの多様な奏法テクニックに対応する必要があります。

**目的**: GuitarSet以外のデータセットを混合学習に取り込むことで、**未知音源への汎化能力を向上**させる。

### 10.2 使用データセット

| データセット | 特徴 | トラック数 | 役割 |
| :--- | :--- | :---: | :--- |
| **GuitarSet** | スチール弦・実演奏・弦別アノテーション | 286 (Train) | ベースライン |
| **GAPS** | ナイロン弦・クラシカルギター・MIDIアノテーション | 371 (うち122使用, 30%混合) | 音色多様性の拡張 |
| **AG-PT-set** | アコースティックギター・12種奏法テクニック | 497 (うち122使用, 30%混合) | 奏法バリエーションの拡張 |

**AG-PT-set（Acoustic Guitar Playing Technique Set）のアノテーション内容:**

- ピッチ系: Bending, Hammer-on, Staccato, Vibrato, Harmonics, Palm Mute
- パーカッシブ系: Kick, Snare, Tom（ボディ打撃系）

### 10.3 学習パイプライン

| Step | 内容 | ソースモデル | 混合データ |
| :---: | :--- | :--- | :--- |
| **Step 2** | GuitarSet直接FT | 合成データ事前学習モデル | GuitarSet (286) のみ |
| **Step 3** | GAPS Multi-task | Step 2 Best | GuitarSet (286) + GAPS (122) |
| **Step 4** | MoE E2E評価 | Step 3 モデル | — (推論のみ) |
| **Step 6** | 3DS統合 | Step 2 Best | GuitarSet (286) + GAPS (122) + AG-PT (122) |
| **Step 7** | 弦分類CNN再学習 | — | GuitarSet 360曲 |
| **Step 8** | 運指LSTM再学習 | — | GuitarSet 360曲 + CNN弦確率 |

**学習ハイパーパラメータ（全Step共通）:**

- 学習率: 3e-6 / Patience: 10 / Max Epochs: 9999 / バッチサイズ: 1
- 検証データ: GuitarSet Validation分割（36曲）

### 10.4 Step 3 結果: GAPS Multi-task統合

全7ドメインについて、GuitarSet + GAPSの混合学習を実施しました。

| ドメイン | Step 2 F1 | Step 3 F1 | 差分 | Epochs | 備考 |
| :--- | :---: | :---: | :---: | :---: | :--- |
| martin_finger | 0.7774 | 0.7710 | -0.0064 | 96 (ES) | |
| taylor_finger | 0.7784 | 0.7724 | -0.0060 | 49 (ES) | |
| **gibson_thumb** | 0.7840 | **0.7892** | **+0.0052** | 38 (ES) | ✅ Step 2超え |
| **luthier_finger** | 0.7802 | **0.7814** | **+0.0012** | 27 (手動) | ✅ Step 2超え |
| **luthier_pick** | 0.7849 | **0.7924** | **+0.0075** | 12 (手動) | ✅ Step 2超え・最大改善 |
| **martin_pick** | 0.7892 | **0.7916** | **+0.0024** | 13 (手動) | ✅ Step 2超え |
| **taylor_pick** | 0.7866 | **0.7921** | **+0.0055** | 10 (手動) | ✅ Step 2超え |
| **平均** | **0.7830** | **0.7843** | **+0.0013** | | **5/7ドメインで改善** |

> **注記**: 上記F1はGuitarSet Validation分割（36曲, onset_threshold=0.5）での値。「手動」はBest F1が十分と判断しEarly Stop前に手動停止したもの。

**傾向分析:**

- **Pick系（luthier_pick, martin_pick, taylor_pick）が顕著に改善** — GAPSのナイロン弦データがPick奏法のアタック検出を補強した可能性
- **Finger系（martin_finger, taylor_finger）はわずかに低下** — GAPSデータとの音色ドメインギャップが影響した可能性
- gibson_thumbは例外的にFinger系でも改善 — 親指奏法がナイロン弦のソフトなアタックと親和性が高い

### 10.5 Step 4 結果: MoE E2Eベンチマーク（Step 3モデル使用）

Step 3完了後、7つのmultitaskモデルを使用してGuitarSet Test分割10曲でMoE E2E評価を実施。

| 指標 | Step 3モデル | Step 2モデル（参考） | 変化 |
| :--- | :---: | :---: | :---: |
| **Pitch F1** | **0.8351** | 0.8310 | **+0.0041** |
| Precision | 0.8699 | 0.8417 | +0.0282 |
| Recall | 0.8030 | 0.8257 | -0.0227 |
| 弦+フレット正解率（検出ノート中） | 94.32% | — | — |
| E2E完全一致率（全GT比） | 75.74% | — | — |

> **注記**: Step 2モデルのF1=0.8310はTest分割36曲での値。Step 3の0.8351はTest分割10曲サブセットでの値。サンプル数の違いにより厳密な比較は不可。Recall低下（-0.0227）はGAPS混合によるonset閾値感度の変化の可能性（未検証）。

### 10.6 Step 6: 3DS統合（完了）

GuitarSet + GAPS + AG-PT-setの3データセット統合学習を全7ドメインで実施。

**最終結果（2026-05-05 17:38 — 全7ドメイン完了）:**

| ドメイン | Best F1 | Step 2 | Step 3 | Step 2→6 | 状態 |
| :--- | :---: | :---: | :---: | :---: | :--- |
| martin_finger | **0.7813** | 0.7774 | 0.7710 | **+0.0039** | ✅ ES@36 |
| taylor_finger | **0.7743** | 0.7784 | 0.7724 | -0.0041 | ✅ ES@31 |
| gibson_thumb | **0.7891** | 0.7840 | 0.7892 | **+0.0051** | ✅ ES@55 |
| luthier_finger | **0.7817** | 0.7802 | 0.7814 | **+0.0015** | ✅ ES@34 |
| luthier_pick | **0.7967** | 0.7849 | 0.7924 | **+0.0118** | ✅ ES@25 |
| martin_pick | **0.7929** | 0.7892 | 0.7916 | **+0.0037** | ✅ ES@40 |
| taylor_pick | **0.7906** | 0.7866 | 0.7921 | **+0.0040** | ✅ ES@25 |
| **平均** | **0.7867** | **0.7830** | **0.7843** | **+0.0037** | **7/7完了** |

> **注記**: 7ドメイン中6ドメインでStep 2超え（taylor_fingerのみ-0.0041）。平均F1は Step 2: 0.7830 → Step 3: 0.7843 → **Step 6: 0.7867（+0.0037）** と段階的に向上。3データセット統合の有効性を確認。

### 10.7 Step 8: 運指LSTM再学習（完了）

Step 7で再学習された弦分類CNN（Val精度92.66%）のCNN弦確率を入力特徴量として、運指LSTM（Bi-LSTM弦予測モデル）を再学習しました。

| 項目 | 値 |
| :--- | :--- |
| 学習データ | GuitarSet 360シーケンス、62,476ノート |
| CNN確率注入率 | 61,885/62,476（99.1%） |
| 検証分割 | LOPO: Train 300 / Val 60（Player 05） |
| エポック数 | 100（Best: Epoch 47） |
| **Best Val精度** | **98.31%** |
| モデルサイズ | 2,236KB |

> **注記**: CNN確率なし版（04/28学習、100エポック）のVal精度は98.92%であり、CNN確率統合版の方が0.61%低い。これはCNN確率自体のノイズによるものと考えられるが、推論時にはCNNとLSTMの判断を融合できるため実環境での堅牢性向上が期待される（定量的検証は未実施）。

### 10.8 モデル選択ロジック

MoE推論時のモデル選択は**最新ステージ優先**で固定選択されます（F1比較によるBest選択ではない）。

```python
# 優先順位: 3DS(Step 6) > multitask(Step 3) > guitarset_ft(Step 2) > 元モデル
stage_suffixes = ["multitask_3ds", "multitask", "guitarset_ft", "model"]
```

**設計判断**: GuitarSet Validation F1が多少低下しても、多データセット混合による未知音源への汎化能力向上を優先する。最終判断はMoE E2E Test評価で行う。

### 10.9 全パイプライン完了サマリー

| Step | 内容 | 状態 | 主要結果 |
| :---: | :--- | :---: | :--- |
| 1 | martin_finger GAPS Multi-task | ✅ | F1=0.7710 |
| 2 | 全7ドメイン GuitarSet FT | ✅ | 平均F1=0.7830 |
| 3 | 全7ドメイン GAPS Multi-task | ✅ | 平均F1=0.7843（+0.0013） |
| 4 | MoE E2E評価 | ✅ | Pitch F1=0.8351 |
| 5 | AG-PT-set前処理 + martin_finger 3DS | ✅ | F1=0.7813 |
| 6 | 全7ドメイン 3DS統合 | ✅ | 平均F1=0.7867（+0.0037） |
| 7 | 弦分類CNN再学習 | ✅ | Val精度92.66% |
| 8 | 運指LSTM再学習（CNN確率統合） | ✅ | Val精度98.31% |

### 10.10 3DSモデル用グリッドサーチ（2026-05-05実施）

3DSモデル（7エキスパート）に対し、推論パラメータの再最適化を実施しました。

**グリッド**: vote_threshold=[3,4,5,6,7] × onset_threshold=[0.5-0.9] × vote_prob_threshold=[0.4,0.5] = 50通り

**グリッドサーチ結果（10曲サブセットA — 均等サンプリング）:**

| 順位 | vote | onset | vprob | F1 | P | R |
| :---: | :---: | :---: | :---: | :---: | :---: | :---: |
| 1 | **7** | 0.8 | 0.4 | **0.8622** | 0.8592 | 0.8653 |
| 2 | 7 | 0.8 | 0.5 | 0.8598 | 0.8777 | 0.8426 |
| 3 | 7 | 0.5 | 0.5 | 0.8594 | 0.8698 | 0.8493 |
| 4 | 7 | 0.7 | 0.4 | 0.8591 | 0.8433 | 0.8755 |
| 5 | 6 | 0.8 | 0.5 | 0.8585 | 0.8485 | 0.8688 |

**E2Eベンチマーク検証（10曲サブセットB — benchmark_e2e.py）:**

| vote | vprob | P | R | F1 | 弦+フレット | E2E完全一致 |
| :---: | :---: | :---: | :---: | :---: | :---: | :---: |
| 5 | 0.5 | 0.8580 | 0.7992 | **0.8276** | 88.96% | **71.63%** |
| 5 | 0.4 | 0.8464 | 0.8046 | 0.8250 | 89.04% | 71.63% |
| 6 | 0.4 | 0.8595 | 0.7909 | 0.8238 | 89.04% | 70.42% |
| 7 | 0.4 | 0.8812 | 0.7506 | 0.8107 | 89.16% | 66.92% |

> **知見**: グリッドサーチ（サブセットA）ではvote=7が最適だったが、E2Eベンチマーク（サブセットB）ではvote=5が最良。サンプリングの違いにより結果が乖離した。

**追加最適化: ドメイン別Best F1モデル選択 + onset微調整:**

最新ステージ固定選択の代わりに、各ドメインのtraining_logからBest F1を読み取り最良ステージを自動選択するロジックを実装。さらにonset_thresholdを0.05刻みで微調整。

選択結果:

- 5/7ドメイン: `multitask_3ds`（3DS）を選択
- taylor_pick: `multitask`（Step 3, F1=0.7921 > 3DS 0.7906）を選択
- gibson_thumb: `multitask`（Step 3, F1=0.7892 > 3DS 0.7891）を選択

| 構成 | onset | P | R | **F1** | E2E完全一致 |
| :--- | :---: | :---: | :---: | :---: | :---: |
| 全3DS固定, onset=0.8 | 0.8 | 0.8580 | 0.7992 | 0.8276 | 71.10% |
| ドメイン別Best, onset=0.8 | 0.8 | 0.8607 | 0.8038 | 0.8313 | 71.56% |
| **ドメイン別Best, onset=0.75** | **0.75** | **0.8543** | **0.8114** | **0.8323** | **72.09%** |

**最終決定**: ドメイン別Best F1選択 + vote=5, onset=0.75, vprob=0.5。
F1: 0.8276 → **0.8323（+0.0047）** 改善。

### 10.11 全360曲フルベンチマーク（2026-05-06 00:20完了）

最適パラメータ（ドメイン別Best F1選択 + vote=5, onset=0.75, vprob=0.5）で全360曲を評価。

| 指標 | 全360曲 | 10曲サブセット | 差異 |
| :--- | :---: | :---: | :---: |
| **Pitch F1** | **0.8324** | 0.8323 | +0.0001 |
| Precision | 0.8148 | 0.8543 | -0.0395 |
| Recall | **0.8509** | 0.8114 | +0.0395 |
| 弦+フレット一致率 | **92.49%** | 88.85% | +3.64pp |
| E2E完全一致率 | **78.70%** | 72.09% | +6.61pp |
| TP (pitch) | 53,161 | 1,067 | — |

> **注記**: 全360曲にはTrain/Val分割の曲が含まれるため、過大評価の可能性あり。ただしF1自体は10曲サブセットとほぼ一致（0.8324 vs 0.8323）しており、サンプリングバイアスが小さいことが確認された。弦+フレット一致率は10曲サブセットが低めに出ており（88.85% vs 92.49%）、サブセット選定が弦精度の低い曲に偏っていた可能性がある。

### 10.12 CNN弦分類器データ拡張実験（2026-05-06 00:45）

既存の軽度拡張（±2bin, ±1frame, noise=0.02, gain=±20%）を以下に強化して100エポック再学習：

- 周波数シフト: ±2→±3ビン
- 時間ジッター: ±1→±2フレーム
- **SpecAugment風マスキング追加**（周波数/時間軸各30%確率）
- ノイズ: 0.02→0.04
- ゲイン: ±20%→±30%

| モデル | Val精度 | E2E弦+フレット | E2E完全一致 |
| :--- | :---: | :---: | :---: |
| 旧CNN（軽度拡張、50エポック） | **92.66%** | **88.85%** | **72.09%** |
| 新CNN（強化拡張、100エポック） | 90.05% | 86.69% | 70.34% |

> **結論**: 強化拡張は**逆効果**。Val精度が2.61pp低下し、E2E弦+フレットも2.16pp低下。SpecAugmentのマスキングが弦識別に重要な倍音情報を破壊した可能性がある。旧CNNモデルに復元済み。

### 10.13 Gradient Accumulation再学習（2026-05-06 00:53開始）

3DSモデルを初期重みとし、Gradient Accumulation（accumulation_steps=4）で全7ドメインを再学習。

| パラメータ | 値 |
| :--- | :--- |
| batch_size | 1 |
| accumulation_steps | 4 |
| **実効バッチサイズ** | **4** |
| 初期重み | 各ドメインの3DS（Step 6）モデル |
| データセット | GuitarSet + GAPS + AG-PT（3DS統合） |
| LR | 3e-6 |
| patience | 15 |
| 出力suffix | `multitask_3ds_ga` |

**期待効果**: 勾配ノイズ低減により収束安定化、Val F1の+0.003〜0.01向上。

**速度最適化（2026-05-06 06:45適用）:**

- batch_size=1→2、AMP（Mixed Precision）有効化
- 1エポック約21分→19分（約10%改善）
- 7ドメイン推定所要時間: 約67時間（2.8日）

**Conformerテスト（2026-05-06 06:22〜断念）:**

- `architecture.py`にConformer実装済み（torchaudio.models.Conformer、67.1Mパラメータ）
- RTX 4060 Ti (8GB)では長い曲でCUDA OOM発生 → **8GB VRAMでは運用不可と判断**
- GRU（約2Mパラメータ）の33倍のモデルサイズが原因

**状態**: 完了（GA再学習はSynth V2混合実験に統合、10.14参照）。

### 10.14 Synth V2混合学習実験（2026-05-09 完了）

#### 10.14.1 動機

個別モデルのVal F1改善が0.77-0.79で頭打ちとなる中、**MoEアンサンブルの合議品質を向上させる**アプローチに転換しました。個別モデルのGuitarSet特化度を下げ、合成データ（Synth V2）による正確なラベルを混合学習することで、各モデルの「間違い方」を多様化し、合議によるノイズ除去効果を高めることを狙いました。

#### 10.14.2 実験構成

| パラメータ | 値 |
| :--- | :--- |
| 初期重み | 各ドメインの3DS（Step 6）モデル |
| データセット | GuitarSet (286) + GAPS (371) + Synth V2 (286/5000) |
| Synth V2サンプリング | 5000件から286件をエポックごとにランダム抽出 |
| Combined Train | 943件/エポック |
| エポック数 | 3（E1でBestが出る傾向を確認済み） |
| LR | 3e-6 |
| 出力suffix | `multitask_3ds_ga` |

> **注記**: AG-PT-setは除外し、代わりにSynth V2（100%正確ラベル）を混合。GAPSは全371件を使用（Step 6の122件から増加）。

#### 10.14.3 個別モデル結果

| ドメイン | Step 6 F1 | Synth V2 Best F1 | 差分 | Best E |
| :--- | :---: | :---: | :---: | :---: |
| martin_finger | 0.7813 | 0.7734 | -0.0079 | E2 |
| taylor_finger | 0.7743 | 0.7522 | -0.0221 | E1 |
| luthier_finger | 0.7817 | 0.7629 | -0.0188 | E1 |
| martin_pick | 0.7929 | 0.7775 | -0.0154 | E1 |
| taylor_pick | 0.7906 | 0.7735 | -0.0171 | E1 |
| luthier_pick | 0.7967 | 0.7834 | -0.0133 | E1 |
| gibson_thumb | 0.7891 | 0.7735 | -0.0156 | E1 |
| **平均** | **0.7867** | **0.7709** | **-0.0157** | |

> **観察**: 全7ドメインでStep 6を下回る。Synth V2 + GAPSの混合により、GuitarSet val最適点からモデルが離れた。しかし「GuitarSetへの過適合が緩和された」とも解釈できる。

#### 10.14.4 MoE E2E ベンチマーク結果

7つのSynth V2混合モデルを使用し、GuitarSet Test分割（Player 05、60曲）でMoE E2Eベンチマークを実施。

| 指標 | Step 6 (3DS) | **Synth V2混合** | **差分** |
| :--- | :---: | :---: | :---: |
| **Pitch F1** | 0.8839 | **0.8877** | **+0.0038** |
| Precision | 0.8592 | 0.8753 | +0.0161 |
| Recall | 0.8653 | 0.9005 | +0.0352 |
| 弦+フレット一致率 | 92.31% | 92.38% | +0.07pp |
| E2E完全一致率 | 82.36% | **83.19%** | **+0.83pp** |

#### 10.14.5 考察と知見

**核心的発見: 個別F1が低下してもMoE F1は向上する。**

- 個別モデルの平均Val F1: 0.7867 → 0.7709（**-0.0157**）
- MoE Test F1: 0.8839 → 0.8877（**+0.0038**）

この逆転現象は、MoEアンサンブルの本質を示しています：

1. **多様性の価値**: GuitarSetに特化したモデルは同じ間違いをする。Synth V2で汎化されたモデルは異なる間違いをするため、合議でノイズが相殺される。
2. **Recallの大幅向上（+0.035）**: 合成データの正確なラベルにより、各モデルが「音を見逃さない」方向に学習。7モデル中5モデルの合意があれば採用されるため、個々のRecall向上がMoE全体に直結。
3. **Precisionも向上（+0.016）**: 各モデルの誤検出パターンが多様化し、偶発的な多数合意が減少。

> **設計指針**: MoEアンサンブルでは、**個別モデルのベンチマーク最適化よりも、モデル間の多様性確保が重要**である。データセット構成を変えることが最も効果的な多様性獲得手段であることが実証された。

### 10.15 全ステージ統合MoE実験（2026-05-09 完了）

#### 10.15.1 動機

Synth V2混合による「多様性→合議改善」効果を確認した上で、**過去の全学習ステージのモデルを同時に合議させる**ことで、さらなる多様性の効果を検証しました。

#### 10.15.2 実験構成

全7ドメイン × 5ステージ = **35モデル**を同時に合議：

| ステージ | 学習データ | モデル数 |
| :--- | :--- | :---: |
| `model` | 合成データのみ（事前学習） | 7 |
| `guitarset_ft` | + GuitarSet FT | 7 |
| `multitask` | + GAPS混合 | 7 |
| `multitask_3ds` | + GAPS + AG-PT混合 | 7 |
| `multitask_3ds_ga` | + GAPS + Synth V2混合 | 7 |
| **合計** | | **35** |

#### 10.15.3 Vote Threshold スイープ結果

| vote | F1 | Precision | Recall | 備考 |
| :---: | :---: | :---: | :---: | :--- |
| 10 | 0.8728 | 0.8341 | 0.9153 | 過少（ノイズ多い） |
| 15 | 0.8829 | 0.8575 | 0.9099 | |
| 17 | 0.8876 | 0.8680 | 0.9081 | ≈ 7モデルSynth V2 |
| 18 | 0.8890 | 0.8726 | 0.9060 | |
| 19 | 0.8901 | 0.8773 | 0.9033 | |
| 20 | 0.8906 | 0.8814 | 0.8999 | |
| **21** | **0.8916** | **0.8864** | **0.8968** | **🏆 最高** |
| 22 | 0.8915 | 0.8916 | 0.8915 | P=R完全均衡 |
| 23 | 0.8903 | 0.8966 | 0.8841 | |
| 24 | 0.8875 | 0.9018 | 0.8736 | |
| 25 | 0.8830 | 0.9081 | 0.8593 | 過多（Recall低下） |

#### 10.15.4 段階的改善の比較

| 構成 | モデル数 | 最適vote | F1 | 累積改善 |
| :--- | :---: | :---: | :---: | :---: |
| Step 6 (GuitarSet特化) | 7 | 5/7 (71%) | 0.8839 | — |
| Synth V2混合 | 7 | 5/7 (71%) | 0.8877 | +0.0038 |
| **全ステージ統合** | **35** | **21/35 (60%)** | **0.8916** | **+0.0077** |

#### 10.15.5 考察

1. **全ステージのモデルが合議に貢献**: 合成データのみの事前学習モデル（`model`）から最新のSynth V2混合モデル（`multitask_3ds_ga`）まで、各ステージは異なるデータで学習しているため、異なる誤りパターンを持つ。35モデルの合議により、個々のモデルの弱点が効果的に相殺された。

2. **最適vote比率の変化**: 7モデル時の最適は5/7（71%）だったが、35モデルでは21/35（60%）が最適。モデル数が増えると多数決の統計的信頼性が上がるため、合意基準を緩められることを示す。

3. **vote=22でP=R=0.8915の完全均衡**: Precision（誤検出抑制）とRecall（見逃し防止）が完全に均衡する点が存在。これは検出システムの理想的な動作点を示す。

4. **推論コスト**: 35モデルの順次推論は1トラックあたり約10秒（RTX 4060 Ti）。7モデル時の約5倍だが、実用上許容範囲内。

> **結論**: 学習プロセスで生成された全モデルを「捨てずに合議させる」ことで、追加の学習コストなしにF1を+0.0077改善できた。これは、**モデルの多様性がアンサンブル品質の最も直接的な決定因子**であるという知見をさらに強化する結果である。

---

## 11. 検証ポイントと改善点

### 11.1 確認済み事実（検証済み）

| # | 項目 | 検証方法 | 結果 |
| :---: | :--- | :--- | :--- |
| 1 | Step 2 全7ドメイン GuitarSet FT完了 | training_log.txt / Early Stop確認 | 平均F1=0.7830 |
| 2 | Step 3 全7ドメイン GAPS Multi-task完了 | training_log.txt / Early Stop確認 | 平均F1=0.7843（+0.0013） |
| 3 | Step 3で5/7ドメインがStep 2を超過 | Validation F1の直接比較 | Pick系が顕著に改善 |
| 4 | Step 4 MoE E2E Pitch F1 向上 | benchmark_e2e.py（10曲） | 0.8310→0.8351（+0.0041） |
| 5 | Early Stop resumeバグ修正 | コード修正後の動作確認 | ログ内"Early stop"行を検知しスキップ |
| 6 | AMP（FP16混合精度）の効果なし | 実測（11s/b vs 通常1-2s/b） | バッチサイズ1では逆効果 |
| 7 | cudnn.benchmark=Trueでハング | 実測（18分経過しE1未完了） | 削除済み |
| 8 | pin_memory=Trueで速度低下 | 実測（35s/bスパイク発生） | 削除済み |
| 9 | num_workers≧1でWindowsエラー | RuntimeError: pickle不可 | ローカルクラス_SynthDSが原因 |
| 10 | CNN弦分類器 Val精度 92.66% | GuitarSet 360トラック学習 | Solo一致率96.60% |

### 11.2 未検証・不明点

| # | 項目 | 不明な理由 | 検証方法（案） | 状態 |
| :---: | :--- | :--- | :--- | :---: |
| 1 | **Step 3モデルの汎化能力向上** | GuitarSet Val F1のみで評価、未知音源での検証なし | GuitarSet外の音源（ロマンス等）で定性評価 | 未検証 |
| 2 | **Step 4 E2E完全一致率の低下（88.10%→75.74%）** | 10曲 vs 60曲のサンプル数差、評価条件の違いが不明 | 同一60トラックで再評価 | 未検証 |
| 3 | **Recall低下の原因（-0.0227）** | GAPS混合による閾値感度変化の仮説のみ | onset_threshold再スイープ | 未検証 |
| 4 | ~~Step 6 3DS統合の最終効果~~ | ~~学習中~~ | ~~全7ドメイン完了後にMoE E2E再評価~~ | ✅ 解決（10.6参照） |
| 5 | **AG-PT-set追加の個別貢献** | GAPS + AG-PTの混合効果が不分離 | AG-PTのみ混合のアブレーション | 未検証 |
| 6 | ~~3DS Best F1 vs Step 3 Best F1~~ | ~~全ドメイン未完了~~ | ~~Step 6完了後に全比較~~ | ✅ 解決（10.6参照） |
| 7 | ~~弦分類CNN再学習（Step 7）の効果~~ | ~~未実行~~ | ~~Step 6完了後に実行・評価~~ | ✅ 解決（Step 7完了） |
| 8 | **モデル選択（最新ステージ優先）の妥当性** | F1ベース選択との比較未実施 | 両方式でMoE E2E比較 | 未検証 |
| 9 | **運指LSTM CNN統合版の実環境効果** | Val精度のみで評価 | E2E弦+フレット一致率での比較 | 未検証 |

### 11.3 学習インフラの改善点

| # | 問題 | 現状 | 改善案 | 優先度 |
| :---: | :--- | :--- | :--- | :---: |
| 1 | **バッチサイズ1がボトルネック** | RTX 4060 Ti 8GB VRAM制約 | Gradient Accumulation（実効バッチサイズ4-8） | 高 |
| 2 | **num_workers=0（シリアルI/O）** | _SynthDSがローカルクラスでpickle不可 | _SynthDSをモジュールレベルクラスに移動 | 高 |
| 3 | **1エポック15-20分（3DS: 730バッチ）** | GPU 100%だがI/O待ちの可能性 | 特徴量の事前キャッシュ（メモリにプリロード） | 中 |
| 4 | **手動スキップの運用負荷** | Best F1が十分と判断し手動停止 | Auto-skip: N epoch以上かつBest F1>閾値で自動停止 | 低 |
| 5 | **ゴーストプロセス問題** | パイプライン停止時にPythonプロセスが残留 | パイプラインスクリプトにクリーンアップ処理追加 | 中 |

### 11.4 モデル・手法の改善点

| # | 領域 | 現状の限界 | 改善案 | 期待効果 |
| :---: | :--- | :--- | :--- | :--- |
| 1 | **音検出（E2Eボトルネック）** | F1=0.8351（Step 3後） | 合成データの品質向上（SoundFont→Neural Synthesis） | F1+0.02-0.05 |
| 2 | **Finger系ドメインの低下** | GAPS混合でmartin/taylor_fingerが低下 | ドメイン別混合比率の最適化（Finger系はGAPS比率を下げる） | F1低下の抑制 |
| 3 | **弦分類器の汎化** | LOO CV: 80.92%（全データ: 96.60%） | データ拡張（ピッチシフト、ノイズ追加）/ 追加データセット | +5-10% |
| 4 | **Vote threshold固定** | vote=5で全ジャンル統一 | ジャンル適応型vote threshold | Funk F1改善 |
| 5 | **モデル選択戦略** | 最新ステージ固定選択 | ドメイン別Best F1選択（Step 2/3/6の最良を使用） | 平均F1+0.003 |
| 6 | **Onset threshold** | 0.8固定（グリッドサーチ結果） | マルチデータセット学習後の再スイープ | Recall回復 |
| 7 | **アーキテクチャ** | CRNN (BiGRU-768) | Self-Attention層の追加 / Conformer化 | 長期的改善 |

### 11.5 論文記載の正確性に関する注記

| # | 箇所 | 問題 | 対処状況 |
| :---: | :--- | :--- | :--- |
| 1 | セクション4.2 全360曲F1=0.9098 | Single Modelの全曲評価値（Train含む）。データリーク注記あり | ✅ セクション4.2で注記済み |
| 2 | セクション7.6 Test F1=0.8310 | Step 2モデルでの結果。Step 6モデルではE2E F1=0.8276（10曲） | ✅ セクション12.4で3DSモデルの評価結果を記載 |
| 3 | セクション9 E2E完全一致率88.10% | Player 05のみ（60トラック）。全テストセットでの検証が望ましい | ⚠️ 全Test分割36曲での再評価未実施 |
| 4 | セクション3.1「Ultimate Single Conformer」 | 実際はCRNN。命名の紛らわしさについて注記あり | ✅ セクション3.1で注記済み |
| 5 | MoEモデル数の記載 | セクション6-7では「6モデル」、実際の学習は7ドメイン | ⚠️ gibson_thumbの扱いを明確化する必要あり |
| 6 | 運指LSTM | パイプラインに未記載だった | ✅ セクション8.10およびStep 8として追記 |

---

## 12. クロスデータセット評価計画（Step 6完了後に実施）

> **本セクションは実施予定の検証計画メモです。Step 6全7ドメイン完了後に実行してください。**

### 12.1 目的

マルチデータセット統合学習（Step 6: 3DS）後のMoEアンサンブルが、**未知のアコースティックギター音源に対してどの程度汎化するか**を定量的に検証する。

### 12.2 評価データ

| # | データセット | 分割 | トラック数 | 楽器 | 学習に使用？ |
| :---: | :--- | :--- | :---: | :--- | :--- |
| 1 | **GuitarSet** | Test | **36曲** | スチール弦アコギ | ❌ 未使用 |
| 2 | **GAPS** | Test | **30曲** | ナイロン弦クラシカル | ❌ 未使用 |
| | **合計** | | **66曲** | | |

- GuitarSet Test: `_processed_guitarset_data` のtest分割（random_state固定）
- GAPS Test: `D:\Music\datasets\GAPS_DATA\_processed\val_ids.txt` の30トラック

### 12.3 実施手順

```text
Step A: Step 6（3DS統合）全7ドメイン完了を確認
Step B: Step 7（弦分類CNN再学習）完了を確認
Step C: MoE E2E評価（GuitarSet Test 36曲）
  → benchmark_e2e.py で Pitch F1 / P / R / 弦+フレット一致率 / E2E完全一致率
Step D: MoE E2E評価（GAPS Test 30曲）
  → 同じ評価スクリプトをGAPS Test音源に適用
  → GAPS用の評価コード追加が必要（MIDIからGround Truth生成）
Step E: 結果をセクション7.6および本セクションに反映
```

### 12.4 結果（2026-05-05実施）

| 指標 | GuitarSet Test (60曲) | GAPS Test (30曲) |
| :--- | :---: | :---: |
| **Pitch F1** | **0.8839** | **0.7312** |
| Precision | 0.8757 | 0.8142 |
| Recall | 0.8923 | 0.6635 |
| TP (pitch) | 7,776 | 16,517 |
| FP | 1,104 | 3,770 |
| FN | 939 | 8,376 |
| 弦+フレット一致率 | **92.31%** | **70.59%** |
| E2E完全一致率 | **82.36%** | **46.84%** |

> **注記**: GuitarSet TestはPlayer 05（60トラック）での評価。GAPS TestはGAPSのval_ids.txt（30トラック）での評価。いずれもStep 6（3DS）の7ドメインMoEモデルとStep 7再学習済みCNN弦分類器を使用。

### 12.5 比較対象

| モデル | GuitarSet Test F1 | GAPS Test F1 |
| :--- | :---: | :---: |
| Step 2（GuitarSet FTのみ） | 0.8958 (60曲) | 未測定 |
| Step 3（+ GAPS混合） | 0.8351 (10曲) | 未測定 |
| **Step 6（+ GAPS + AG-PT混合）** | **0.8839 (60曲)** | **0.7312 (30曲)** |

### 12.6 考察

**GuitarSet Test（スチール弦アコギ）:**

- Pitch F1=0.8839はStep 2の0.8958から**-0.0119低下**
- 3データセット統合により、GuitarSet特化の精度がわずかに犠牲になった
- 弦+フレット一致率も92.31%（Step 2: 97.91%から-5.6%低下）— CNN弦分類器の再学習（Val精度92.40%）が影響

**GAPS Test（ナイロン弦クラシカルギター）:**

- Pitch F1=0.7312 — **ナイロン弦の未知データに対して有意な汎化を確認**
- Recall=0.6635が低い — vote_threshold=5/7の厳格な合意基準がナイロン弦の柔らかいオンセットに対して過剰に保守的
- 弦+フレット一致率70.59% — CNN弦分類器がGuitarSet（スチール弦）で学習されているため、ナイロン弦の音色ドメインギャップが影響

**総合評価:**

- 3DS統合は**ナイロン弦への汎化能力を獲得**（F1=0.7312 > 0）した一方で、GuitarSetの精度を微減させるトレードオフが発生
- GAPS Recallの改善には、vote_thresholdのドメイン適応またはナイロン弦用のonset_threshold調整が有効と考えられる

### 12.7 論文への記載案

> 本研究では、GuitarSet Test分割（スチール弦60曲、Player 05）およびGAPS Test分割（ナイロン弦30曲）の計90曲の未学習データで評価を行った。3データセット統合学習（GuitarSet + GAPS + AG-PT-set）後のMoEアンサンブルは、GuitarSetでPitch F1=0.8839、GAPSでPitch F1=0.7312を達成し、ナイロン弦クラシカルギターへの汎化能力を実証した。

---

## 13. Step 8: Basic Pitch融合戦略 (Hybrid Transcription Pipeline)

### 13.1 動機

Step 6までのMoEアンサンブルはPitch F1=0.8644（30曲mir_eval評価）に到達したが、Precisionは0.8564にとどまり誤検出が課題であった。一方、SpotifyのBasic Pitchモデル（17Kパラメータ、ONNXRuntime推論）は学習なしでF1=0.8713、Recall=0.9074という高いリコールを示した。両者の特性を融合することでSOTA超えを狙う。

### 13.2 融合戦略の比較

GuitarSet Test（Player 05、30曲）でmir_eval note-level評価を実施。5つの融合戦略をテスト：

| 戦略 | F1 | Precision | Recall | 説明 |
| ------ | ----- | --------- | ------ | ------ |
| BP only | 0.8713 | 0.8422 | 0.9074 | Basic Pitch単独 |
| MoE only | 0.8644 | 0.8564 | 0.8740 | Pure MoE（vote=5/7） |
| Union | 0.8403 | 0.7654 | 0.9316 | 和集合（どちらかが検出） |
| Intersection | 0.8550 | 0.9527 | 0.7762 | 積集合（両方が検出） |
| **Fusion C** | **0.8835** | **0.9531** | **0.8263** | BP検出→MoE確認 |

### 13.3 最適パラメータ

マッチング許容値のスイープで70msが最適と判明：

| マッチング許容値 | F1 | P | R |
| ---------------- | ------ | ------ | ------ |
| 30ms | 0.8580 | 0.9583 | 0.7809 |
| 50ms | 0.8785 | 0.9562 | 0.8159 |
| **70ms** | **0.8835** | **0.9531** | **0.8263** |
| 100ms | 0.8824 | 0.9476 | 0.8284 |
| 150ms | 0.8776 | 0.9412 | 0.8248 |

### 13.4 致命的バグの発見と修正

総点検中に、GA学習パイプラインでGuitarSetのデータ拡張が**無効化**されていたことを発見。

```diff
# train_gaps_multitask.py L366
- **common, **config.DATASET_EVAL_AUGMENTATION_PARAMS,
+ **common, **config.DATASET_TRAIN_AUGMENTATION_PARAMS,
```

これにより、ピッチシフト（±2半音）、タイムストレッチ（0.8x-1.2x）、ノイズ追加、リバーブ、EQ、SpecAugmentの全9種類のデータ拡張が無効であった。拡張有効化により個別モデルF1の大幅改善が見込まれる。

### 13.5 アーキテクチャ

```text
Audio Input
  ├─→ Basic Pitch (ONNX, 高Recall候補検出)
  ├─→ MoE Ensemble (7 CRNNs, 弦/フレット情報付き)
  └─→ 融合: onset差<70ms ∧ pitch差≤1半音 → MoEノートを採用
       → CNN弦分類 → Viterbi DP → Fingering LSTM → GP5出力
```

### 13.6 実装詳細

- `pipeline.py`: Step 3をFusion Cに置換。フォールバック: MoE単独→BP単独
- `dataset.py`: scipy `iirfilter` API互換性修正（`Wn`をリストからスカラーに変更）
- `run_ga_retrain.py`: cp932エンコーディング修正

### 13.7 今後の計画

1. **Phase 1**: 拡張有効GA再学習（7ドメイン、推定24-48時間）
2. **Phase 2**: 改善モデルでの融合パラメータ再最適化
3. **目標**: Fusion F1 = 0.90+ (Precision 0.95+維持)

---

## 14. Step 9: V2.2 総点検と学習基盤改善

### 14.1 動機

Step 8までの実装では、GA再学習パイプラインに複数の致命的バグが内在していた。大規模学習（7ドメイン×15-20エポック、推定24-48時間）の前に、5回の総点検・7件の小テスト・1エポック完走ドライランを実施し、全問題を解消した。

### 14.2 発見・修正したバグ（全8件）

| # | バグ | 影響度 | 発見回 |
| --- | --- | --- | --- |
| 1 | GuitarSetデータ拡張が無効（EVAL_PARAMSを参照） | 致命的 | 1回目 |
| 2 | MoEモデル選択にGA再学習モデルが含まれない | 致命的 | 2回目 |
| 3 | おかわりリトライ時にログ未リセット（Early-stop後SKIP） | 致命的 | 5回目 |
| 4 | scipy `iirfilter` API互換性（`Wn`リスト非対応） | 重要 | 1回目 |
| 5 | cp932エンコーディング（Unicode文字でクラッシュ） | 重要 | 1回目 |
| 6 | mirdata GuitarSetインデックス未ダウンロード | 重要 | 1回目 |
| 7 | pipeline.py 未使用import | 軽微 | 1回目 |
| 8 | **Gradient Clipping未実装**（勾配爆発リスク） | 基礎改善 | 5回目 |

### 14.3 Gradient Clipping の導入

`epoch_processing.py` の学習ループに `clip_grad_norm_(max_norm=1.0)` を追加。

小テストにより、1バッチ学習時の勾配ノルムが **23.2** に達することを確認。これはGRU/BiGRUモデルにおける勾配爆発の典型的パターンであり、クリップなしでは学習が不安定になる原因となっていた可能性がある。

```python
# epoch_processing.py — 非AMPブランチ
torch.nn.utils.clip_grad_norm_(model.parameters(), max_norm=1.0)
optimizer.step()
```

### 14.4 小テスト結果

| # | テスト内容 | 結果 |
| --- | --- | --- |
| 1 | ピッチシフト+2半音ラベル調整 | PASS |
| 2 | ピッチシフト-3半音フィルタ（fret<0除外） | PASS |
| 3 | create_frame_level_labels形状 [T,6] | PASS |
| 4 | GAPS/GuitarSet混合バッチcollate互換性 | PASS |
| 5 | Gradient Clipping動作（norm 23.2→1.0） | PASS |
| 6 | 拡張ランダム性（毎回異なるフレーム数） | PASS |
| 7 | 全7ドメインハイパーパラメータ一貫性 | PASS |

### 14.5 ドライラン結果（martin_finger, 1エポック）

| 項目 | 値 |
| --- | --- |
| Train Loss | 1.2530 |
| Val F1 | **0.7880** |
| Val Precision | 0.8036 |
| Val Recall | 0.7754 |
| 3DS Best F1（比較） | 0.7813 |
| **差分** | **+0.0067** |
| 学習時間 | 約16分 |
| エラー | なし |

> **注目**: Gradient Clipping + データ拡張有効化により、**1エポック目で既に3DS Best F1（0.7813）を超えた**（0.7880）。過去の学習では勾配ノルム23以上の勾配爆発状態で、拡張なしで学習していたことが判明。

### 14.6 考察

- 勾配ノルム23は、バッチサイズ1のRNN学習において極端に高い値であり、学習率3e-6の低設定でも不安定な更新が発生していた可能性がある
- データ拡張（ピッチシフト、タイムストレッチ、ノイズ、リバーブ等）が初めて正しく機能し、汎化性能の底上げに寄与
- 1エポックで+0.0067の改善は、15-20エポックの完走で更なる向上が期待される
